{
  "name": "bluesky-store-locator-root",
  "version": "1.0.0", 
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build && cp -r dist/* ../",
    "dev": "cd client && npm run dev"
  }
}